export interface WalletDataType {
  address: string;
  alias: string;
  walletType: string;
  balance: string;
  nonce: string;
  createdAt: string;
}

export interface TransactionDataType {
  address: string;
  alias: string;
  walletType: string;
  balance: string;
  nonce: string;
  createdAt: string;
}
